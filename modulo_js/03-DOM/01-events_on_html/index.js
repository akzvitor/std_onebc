/**
 * events are dynamic way of executing js in a html page
 * execute js without reload the page
 * they're activated by specific actions on the page
 */

// is triggered when the mouse simply passes over the text box
function onMouseEnter() {
    console.log('mouse enter')
}

// triggered on click
function onClick() {
    console.log('clique no elemento')
}

function onFocus() {

}

function onBlur() {

}